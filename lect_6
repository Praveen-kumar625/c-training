#include <stdio.h>
int main() {
    int n;
    scanf("%d", &n);
    while (n != 0) {
        printf("%d", n % 10); // prints in reverse
        n = n / 10;
    }
    printf("\n");
    return 0;
}